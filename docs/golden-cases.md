# Golden Cases

The golden files are not a benchmark. They are a stable review surface for sandbox.

The main golden fixture is `fixtures/golden/scoreboard.csv`. The matrix fixture is `fixtures/golden/lane-matrix.csv`. Together they cover `capability width`, `resource pressure`, `plugin drift`, and `escape risk` with different score ranges.

The purpose is to make intentional rule changes show up in review.
